const Title = ({ title }: { title: string }) => {
  return (
    <div className='sticky top-0 text-center w-full text-2xl lg:text-3xl font-bold py-6 pb-16 lg:py-12'>{title}</div>
  )
}

export default Title
